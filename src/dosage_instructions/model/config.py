from typing import Any

config: dict[str, Any] = {
    "convert_number": "word_to_digit",
    "dosage_col_name": "dosage",
    "batch_size": 10000000,
    "rebuild_or_append_lookup": "append",
    "fresh_start": True,
}
